A separate local follow-up compares $2^{19}$-entry and $2^{20}$-entry batches,
again with three fresh executions per protocol and configuration.
We selected the additional setting after a one-run pilot of $2^{18}$-entry and $2^{19}$-entry batches.
For \ourprotocol, the median preprocessing times are 15.18\,s and 15.65\,s, respectively,
with observed ranges 15.06--15.34\,s and 14.93--15.88\,s.
The smaller batch sends 274.16\,MiB instead of 221.82\,MiB offline.
Batcher's corresponding medians are 14.60\,s and 14.24\,s.
The observed ranges overlap, so the median difference alone does not establish a consistent speedup.
The main online results retain their original $2^{20}$-entry batch setting.
